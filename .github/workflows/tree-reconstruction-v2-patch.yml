name: Tree Reconstruction V2 patch

on:
  push:
    branches:
      - agent/realistic-tree-presets-v1

permissions:
  contents: write

jobs:
  apply-tree-reconstruction-v2:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/realistic-tree-presets-v1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply Tree Reconstruction V2 integration
        run: node scripts/patch-tree-reconstruction-v2.mjs
      - name: Commit integration
        run: |
          if git diff --quiet -- src/lib/tree-generator.mjs src/lib/raster.mjs; then
            echo "Tree Reconstruction V2 integration already applied"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/tree-generator.mjs src/lib/raster.mjs
          git commit -m "Wire measured crown geometry into tree compiler"
          git push origin HEAD:agent/realistic-tree-presets-v1
